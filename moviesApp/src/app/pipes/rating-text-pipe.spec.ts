import { RatingTextPipe } from './rating-text-pipe';

describe('RatingTextPipe', () => {
  it('create an instance', () => {
    const pipe = new RatingTextPipe();
    expect(pipe).toBeTruthy();
  });
});
